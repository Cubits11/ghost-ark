# Claim/Evidence Matrix

This matrix maps each public Ghost-Ark claim to its allowed wording, forbidden wording, evidence location, and validation status. A claim may only be made in the allowed form, and only while the listed evidence exists and is reproducible.

Status vocabulary:

- `Local evidence` — reproducible on a laptop with zero AWS credentials.
- `Local partial` — local artifacts exist; the claim boundary is narrower than the component name suggests.
- `AWS-synth-only` — infrastructure templates synthesize locally; no live runtime evidence.
- `AWS-required` — the claim cannot be validated without a bounded live AWS window.
- `Not implemented` — no artifact exists; the row is a placeholder boundary.

| Claim ID | Claim | Allowed wording | Forbidden wording | Evidence location | Local command | AWS required? | Status | Limitation |
|---|---|---|---|---|---|---|---|---|
| CLAIM-001 | Canonical receipt hashing | Receipt payloads canonicalize deterministically under Ghost-Ark canonicalization rules, verified by local tests. | "Receipts are tamper-proof" / "hashing is unbreakable" | `packages/receipt-schema/src/hashCanonicalization.ts`, `packages/enforcement-runtime/src/receipts/canonical.ts`, unit tests under `tests/unit/` | `npm test` | No | Local evidence | Canonicalization is deterministic under implemented rules; RFC 8785/JCS compliance is not claimed. |
| CLAIM-002 | Local receipt verification | A local verifier checks canonical receipt identity, payload digest, tenant expectation, and RSA-PSS signature validity for supplied artifacts. | "Verification proves the output is true or safe" | `tools/ghost-verify.mjs`, `examples/sample-receipts/` | `npm run ghost-verify -- --receipt examples/sample-receipts/valid-receipt.json --key examples/sample-receipts/public-key.pem --tenant acme-lab` | No | Local evidence | PASS means internal consistency of supplied artifacts under Ghost-Ark verifier rules only. |
| CLAIM-003 | Malicious receipt rejection | The verifier rejects a corpus of tampered receipts (payload, tenant, digest, algorithm, signature mutations) in local negative tests. | "Prevents all attacks" / "impossible to forge" | Negative-test corpus in `tests/`, `docs/security/RECEIPT_ATTACK_CORPUS.md` | `npm test` | No | Local evidence | Rejection is demonstrated for the enumerated corpus, not for all possible forgeries. |
| CLAIM-004 | Standalone verifier behavior | A standalone Node verifier consumes exported artifacts, imports Node built-ins only, and agrees with the production verifier on the committed adversarial corpus. | "Independently audited" / "trustless verification" / "formally correct verifier" | `verifiers/node/ghost_receipt_verify.mjs`, `tests/differential/nodeIndependentVerifier.test.ts`, `examples/malicious-receipts/manifest.json` | `npm run receipt:verify:agreement` | No | Local evidence | Source-import separation and corpus agreement are local evidence only. The implementation remains in the same repository, follows shared protocol assumptions, and has not received external review (RISK-016). |
| CLAIM-005 | Policy-governed invocation | The enforcement runtime evaluates deterministic policy decisions, fails closed, and emits decision receipts in local tests. | "Guarantees safe model behavior" | `packages/enforcement-runtime/src/runtime/`, `tests/unit/enforcement-runtime/`, `tests/integration/test_governedInvokeLifecycle.test.ts` | `npm test` | No (local path); Yes (deployed path) | Local partial | Local tests exercise mock/local adapters; the deployed Bedrock path requires live AWS evidence. |
| CLAIM-006 | Tenant isolation | Tenant-boundary checks reject client-declared tenant authority and fail closed on mismatch in local tests. | "Tenant isolation is proven" / "multi-tenant secure" | Tenant-boundary unit tests, `infra/` IAM policy templates | `npm test` | Yes | AWS-required | Isolation is not proven. Live API Gateway/Cognito/IAM behavior has no preserved evidence bundle (RISK-003). |
| CLAIM-007 | KMS signing provenance | The runtime supports a KMS signing mode; local tests cover the signing envelope and HMAC dev mode only. | "KMS-proven provenance" from HMAC-dev fixtures | `packages/enforcement-runtime/src/receipts/signer.ts`, signer unit tests | `npm test` | Yes | AWS-required | HMAC-dev fixtures do not prove KMS provenance. Requires live KMS sign/verify evidence (RISK-004). |
| CLAIM-008 | DynamoDB receipt persistence | CDK defines receipt ledger tables and scoped grants; templates synthesize locally. | "Receipts are durably persisted in production" | `infra/cdk/lib/api-stack.ts` | `npx cdk synth` (optional, local synth only) | Yes | AWS-synth-only | Synthesis proves template shape, not live persistence behavior. |
| CLAIM-009 | Merkle checkpointing | Ghost-Ark locally builds signed receipt checkpoints and verifies chain-head inclusion proofs under its implemented tree rules. | "Immutable audit trail" | `packages/enforcement-runtime/src/receipts/checkpoint.ts`, `schemas/receipt-checkpoint.json`, `schemas/receipt-inclusion-proof.json`, focused tests | `npx vitest run tests/unit/enforcement-runtime/receipts/test_checkpoint.test.ts tests/unit/research-frontier/transparencyArtifacts.test.ts` | No (local); Yes (anchored) | Local partial | Local checkpoints are not externally published or anchored to an immutable store. |
| CLAIM-010 | Object Lock transparency bundles | Ghost-Ark documents the artifact and denial evidence required for a future bounded Object Lock validation. | "Write-once transparency log exists" | `docs/operations/runbooks/object-lock-transparency-window.md`, `docs/architecture/transparency-witness-model.md` | Documentation inspection only | Yes | AWS-required | No live Object Lock retention, overwrite-denial, or delete-denial bundle exists (RISK-010). |
| CLAIM-011 | Guardrail observation schema | Ghost-Ark defines and locally validates a privacy-restricted guardrail observation shape and bounded telemetry attribute mapping. | Any claim that live guardrail observations are captured or receipt-bound today | `schemas/research/guardrail-observation.schema.json`, `packages/research-frontier/src/guardrailObservation.ts`, examples and tests | `npx vitest run tests/unit/research-frontier/guardrailObservation.test.ts` | No (schema); Yes (live capture) | Local evidence | Receipt references are declared, not cryptographically verified by this validator; no runtime emitter, storage, or telemetry export is implemented. |
| CLAIM-012 | CC-Framework correlation analysis | Ghost-Ark locally converts receipt-referenced guardrail scores under explicit rules and reports co-failure tables, phi values, Wilson intervals, and pairwise Fréchet bounds. | "Correlated failure is validated" / any claim of causal, stable, or live dependence measurement | `packages/research-frontier/src/ccCorrelation.ts`, `schemas/research/cc-*.schema.json`, `docs/research/CC_CORRELATION_ANALYSIS.md`, example and tests | `npx vitest run tests/unit/research-frontier/ccCorrelation.test.ts` | No (local mechanics); Yes (live data) | Local partial | The adapter has no external CC-Framework service integration and does not validate sampling, calibration, stationarity, causality, or live data provenance. |
| CLAIM-013 | Human review workflow | Ghost-Ark defines and locally validates queue, decision, audit-link, receipt-carry-forward, false-positive, and escalation artifact mechanics. | "Human oversight is in place" | `schemas/research/human-review-*.schema.json`, `packages/research-frontier/src/humanReview.ts`, `docs/research/HUMAN_REVIEW_INCIDENT_WORKFLOW.md`, examples and tests | `npx vitest run tests/unit/research-frontier/humanReview.test.ts` | No (local contract); Yes (operating workflow) | Local evidence | No queue service, reviewer staffing, notification, access control, or operational decision record is evidenced. |
| CLAIM-014 | Incident reporting workflow | Ghost-Ark defines and locally validates a synthetic incident artifact with escalation, evidence, receipt, audit-chain, timeline, and closure invariants. | "Incident response is operational" | `schemas/research/incident-report.schema.json`, `packages/research-frontier/src/humanReview.ts`, `docs/research/HUMAN_REVIEW_INCIDENT_WORKFLOW.md`, examples and tests | `npx vitest run tests/unit/research-frontier/humanReview.test.ts` | No (local contract); Yes (operations) | Local evidence | The examples are synthetic; no incident team, page delivery, containment, recovery, or executed corrective action is evidenced. |
| CLAIM-015 | Control mapping to NIST AI RMF / ISO IEC 42001 | Ghost-Ark provides a versioned candidate evidence crosswalk for NIST AI RMF 1.0 functions and ISO/IEC 42001:2023 clauses 4-10. | "Compliant with NIST AI RMF" / "ISO 42001 aligned" / "certification ready" | `docs/compliance/control-mapping.json`, `schemas/compliance/control-mapping.schema.json`, `docs/compliance/control-matrix.md`, focused tests | `npx vitest run tests/unit/research-frontier/complianceControlMapping.test.ts` | No (mapping); external review required | Local partial | The mapping is not a subcategory audit, Statement of Applicability, legal analysis, conformity assessment, or certification; organizational evidence gaps remain explicit. |
| CLAIM-016 | Repeatable AWS deployment evidence | Ghost-Ark defines a schema, sanitizer, validator, local synth gate, and runbooks for a planned deploy → smoke → capture → destroy lifecycle. Repeatable deployment remains AWS-required. | "Repeatably deployable" without a preserved complete live bundle | `schemas/live-aws-evidence-bundle.schema.json`, `tools/evidence/`, `docs/operations/runbooks/live-aws-evidence-*.md` | `npm run spine:c:local` (local preparation only) | Yes | AWS-required | The local artifacts are preparation evidence. No preserved deploy/destroy evidence bundle exists (RISK-014). |
| CLAIM-017 | Cost-bounded live validation lifecycle | Dedicated runbooks define preflight approval, an authorized time cap, cost-mode review, abort criteria, teardown, and residual-resource confirmation. | "Live validation is routine and safe" | `docs/operations/runbooks/live-aws-evidence-preflight.md`, `docs/operations/runbooks/live-aws-evidence-window.md`, `docs/operations/runbooks/live-aws-evidence-cleanup.md` | `npm run infra:synth` (synthesis only) | Yes | AWS-required | The lifecycle is specified, not executed by these artifacts. No preserved execution record of a complete lifecycle exists (RISK-013). |
| CLAIM-018 | Evidence-bundle lifecycle contract | A Draft 2020-12 schema and local validator enforce the declared synthetic/live boundary, bounded window, scoped observations, receipt-check consistency, cleanup state, and known leak rules for supplied bundle files. | "A schema-valid fixture proves live AWS behavior" / "The sanitizer detects every secret" | `schemas/live-aws-evidence-bundle.schema.json`, `examples/evidence/`, `tools/evidence/`, `tests/unit/evidence/liveAwsEvidenceBundle.test.ts` | `npm run validate:evidence-bundle` | No (contract); Yes (recorded live events) | Local evidence | Maturity L3 for the locally tested validator around an L2 bundle schema. Validation checks the supplied artifact; it does not independently attest to AWS events or detect every sensitive value. |
| CLAIM-019 | Key lifecycle and rotation | Ghost-Ark locally enforces key-manifest verification epochs and a separate ACTIVE-only signing authorization decision, with succession and compromise-response procedures. | "KMS rotation is operational" / "compromised keys cannot sign in AWS" | `schemas/key-manifest.json`, `packages/enforcement-runtime/src/receipts/keyManifest.ts`, `docs/architecture/ADR-0002-key-manifest-lifecycle.md`, rotation runbook and tests | `npx vitest run tests/unit/enforcement-runtime/receipts/test_key_manifest.test.ts` | No (local policy); Yes (live KMS behavior) | Local evidence | The local primitive does not prove signer-path wiring, IAM/key-policy denial, manifest publication, or a completed live rotation. |
| CLAIM-020 | Local witness consistency mechanics | Ghost-Ark locally generates and verifies witness-signed checkpoint consistency artifacts under its research verifier rules. | "Independent witnesses operate" / "decentralized transparency" | `packages/research-frontier/src/witnessCheckpoint.ts`, `schemas/research/witness-*.schema.json`, `examples/research/witness-bundle.local/`, tests and verifier | `npm run research:witness-bundle -- --out examples/research/witness-bundle.local` | No (local mechanics); Yes (independent operation) | Local partial | The witness and fixture are maintainer-controlled. There is no external monitor, independent key operator, publication service, gossip, or Object Lock evidence. |

## Interpretation rules

- A row marked `AWS-required` must not be presented as validated from local tests, fixtures, or CDK synthesis.
- A row marked `Not implemented` is a boundary placeholder; it must not appear in public claims at all.
- `Local evidence` applies only to the narrow allowed wording in that row; it does not imply runtime integration or organizational operation.
- Forbidden wording in this table is quoted only to define what reviewers should reject.
