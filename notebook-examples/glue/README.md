# Glue Notebook Examples

Use these examples only after the dev account bootstrap creates the raw and curated evidence buckets. Notebook sessions should run under centrally owned service roles and tenant-scoped session names.
