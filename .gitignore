node_modules/
dist/
coverage/
cdk.out/
.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.backup
*.tfplan
*.log
__pycache__/
*.pyc
.DS_Store
tmp/

# Artifact-evaluation outputs and caches (regenerated by `make reproduce`).
# Anchored to repo root so the tracked proofs/**/artifacts/ TLC logs are kept.
/artifacts/
/.cache/
docs/dissertation/.build/
docs/dissertation/ghost-ark-usenix.tex
docs/dissertation/ghost-ark-usenix.pdf
# TLC trace-exploration scratch files
proofs/**/*_TTrace_*.tla
proofs/**/*_TTrace_*.bin

# Rust build output (DAB gateway/verifier). Never track compiled artifacts;
# Cargo.lock files ARE tracked (for reproducible, --locked builds).
target/
**/target/
